[general]
ui = TTY.TTYUI
accounts = main
pythonfile = ~/.passmanager
fsync = False


[Account main]
localrepository = main-local
remoterepository = main-remote
status_backend = sqlite
#postsynchook = notmuch new


[Repository main-local]
type = Maildir
localfolders = ~/.mail/igor@kalnitsky.org
nametrans = lambda folder: {
    'drafts':  '[Gmail]/Drafts',
    'sent':    '[Gmail]/Sent Mail',
    'flagged': '[Gmail]/Starred',
    'trash':   '[Gmail]/Trash',
    'archive': '[Gmail]/All Mail',
    'notify':  'Notify',}.get(folder, folder)


[Repository main-remote]
maxconnections = 1
type = Gmail
remoteuser = igor@kalnitsky.org
remotepasseval = get_password("http://imap.gmail.com/", "igor@kalnitsky.org")
realdelete = no
cert_fingerprint = 6d1b5b5ee0180ab493b71d3b94534b5ab937d042

nametrans = lambda folder: {
    '[Gmail]/Drafts':    'drafts',
    '[Gmail]/Sent Mail': 'sent',
    '[Gmail]/Starred':   'flagged',
    '[Gmail]/Trash':     'trash',
    '[Gmail]/All Mail':  'archive',
    'Notify':            'notify',}.get(folder, folder)

folderfilter = lambda folder: folder not in [
    '[Gmail]/Trash',
    '[Gmail]/Important',
    '[Gmail]/Spam',]
