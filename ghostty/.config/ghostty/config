theme = Nord
font-family = TvirMono Nerd Font
font-size = 14
font-feature = -calt, -liga, -dlig
auto-update = off
copy-on-select = true
maximize = true
notify-on-command-finish = unfocused
notify-on-command-finish-action = bell, notify
window-theme = ghostty
config-file = ?config.local
